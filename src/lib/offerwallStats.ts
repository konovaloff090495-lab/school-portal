/**
 * Социальное доказательство для витрины подарков — ТОЛЬКО настоящие цифры.
 *
 * Источник — Яндекс.Метрика портала (счётчик 108789843): посетители за 30 дней и
 * сумма достижений целей «Заявка с карточки школы» (601419050) и «Заявка из поп-апа»
 * (601419042). Их тянет /api/offerwall/stats с часовым кэшем; когда токена нет или
 * API недоступен — показываются значения ниже, замеренные вручную.
 *
 * Выдуманные числа («больше 3 000 человек уже выбрали») на витрине запрещены:
 * это недостоверная реклама, а реальные цифры портала и так крупные.
 */

export interface WallStats {
  /** Заявок за последние 30 дней */
  leads30: number
  /** Посетителей за последние 30 дней */
  users30: number
  /** Когда цифры замерены (ISO-дата) */
  measuredAt: string
}

export const FALLBACK_STATS: WallStats = {
  leads30: 371,
  users30: 32053,
  measuredAt: '2026-09-24',
}
