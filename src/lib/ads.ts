/**
 * Блоки РСЯ площадки 19425636 (pro-schools.ru).
 *
 * Один блок = одно направление сайта. Статистика РСЯ режется по НАЗВАНИЮ блока
 * в кабинете (`block_caption`), поэтому смешивать направления в одном блоке нельзя —
 * иначе не видно, что зарабатывает блог, а что ГДЗ.
 *
 * Посмотреть доход в разрезе блоков:
 *   python3 rsya.py --period 30days --by block_caption
 *
 * ЗАМЕР 31.08.2026 (неделя 24–31.08): запросов 1 739 при 2 279 монетизируемых
 * просмотрах — 0,76 на просмотр. Дыра найдена: в /uchebnik/ единственное место
 * жило в сайдбаре `hidden lg:block`, и 52% трафика раздела (телефоны и планшеты,
 * 2 207 просмотров за 30 дней) не давали НИ ОДНОГО запроса. Поставили второе
 * место в теле страницы под мобильные (см. YandexRTBBanner, prop `viewport`).
 * Мобильный CPM 152₽ против десктопного 57₽, так что раздел недобирал вдвойне.
 *
 * ЗАМЕР 18.08.2026 (неделя после правки pageNumber от 10.08): запросов на
 * монетизируемый просмотр 0,82 против 0,89 неделей раньше — pageNumber повторные
 * места НЕ оживил. Это ожидаемо: pageNumber в РСЯ означает «следующая страница
 * бесконечной ленты», а не «второе место на том же экране». Единственный способ
 * получить 2–3 запроса с просмотра — завести в кабинете ОТДЕЛЬНЫЕ блоки и вписать
 * их ID ниже вместо -2 и -3. Пока этого нет, потолок раздела — один показ на просмотр.
 *
 * ВНИМАНИЕ (замер 10.08.2026): в кабинете РСЯ заведён только ОДИН блок —
 * «Баннер (13.06.2026)», вся статистика идёт одной строкой. Идентификаторы -2 и -3
 * ниже отдельными блоками не являются, поэтому разрезать доход по направлениям
 * сейчас нельзя, а повторные места на странице оживают только через pageNumber
 * (см. YandexRTBBanner). Чтобы разрез заработал по-настоящему, нужно завести
 * в кабинете отдельные блоки под каждое место и вписать их реальные ID сюда.
 *
 * Каталог школ (/shkoly/, /shkola/) рекламы НЕ содержит — это лидогенерация,
 * плюс место под платные спецразмещения партнёров. Не добавлять.
 */
export const AD_BLOCKS = {
  /** «БЛОГ — в тексте»: 2 блока в теле статьи, все экраны */
  blogInline: 'R-A-19425636-2',
  /** «БЛОГ — сайдбар»: боковая колонка статьи, только десктоп */
  blogSidebar: 'R-A-19425636-3',
  /** «ГДЗ и учебник»: 9 мест в разделах /gdz/ и /uchebnik/ */
  gdzUchebnik: 'R-A-19425636-1',
} as const
